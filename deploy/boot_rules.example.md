# Cortex boot rules (example)

> Copy to `<cortex_home>/boot_rules.md` and edit. Injected into a fresh cortex
> window at SessionStart when the file exists. Absent = nothing injected.

- Placeholder: the user authors the real sleep/wake rules here.
- On wake: read the wakeup note named in your first prompt, then act on it.
- End each turn by choosing a wait or a next wake time; lie_down when done.
